export const metadata = {
  title: 'What is AR.IO?',
  description:
    "AR.IO is the world's first permanent cloud network, providing infrastructure and services to make Arweave data easily accessible and usable",
  openGraph: {
    title: 'What is AR.IO?',
  },
}

# What is AR.IO?

AR.IO is the world's first permanent cloud network, providing the essential infrastructure and services needed to make Arweave's permanent storage easily accessible and usable for developers, creators, and end users. While Arweave handles the core storage functionality, AR.IO adds the cloud services layer that transforms raw storage into a user-friendly, web-accessible platform. Think of it as the "AWS for the permanent web" - providing all the services you need to build and deploy applications on Arweave.

The AR.IO Network consists of a global network of gateways that act as "Permanent Cloud Service Providers." These gateways provide fast, reliable access to data stored on Arweave, handle domain name resolution through the Arweave Name System (ArNS), and offer additional services like data indexing, querying, and content delivery. Each gateway is independently operated and incentivized through the ARIO token, creating a decentralized yet coordinated ecosystem that ensures data remains accessible even if individual gateways go offline.

AR.IO gateways make Arweave data accessible through familiar web protocols. Instead of needing to know complex transaction IDs, you can access your data through friendly URLs like `https://arweave.net/your-data` or even custom domain names. The network also provides powerful APIs for querying and searching data, making it easy to build applications that can discover and interact with permanent content. This transforms Arweave from a storage network into a full-featured platform for building the permanent web.

The network is designed to be open, permissionless, and community-driven. Anyone can run a gateway, and the ARIO token provides economic incentives for maintaining high-quality service. This creates a sustainable model where the network grows stronger as more people participate, ensuring that the permanent web remains accessible and reliable for generations to come.
