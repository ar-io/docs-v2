import { CodeGroup } from '@/components/Code'
import { Table } from '@/components/Table'

export const metadata = {
  title: 'getDistributions',
  description:
    'Get the rewards distribution information for the specified epoch',
  openGraph: {
    title: 'getDistributions',
  },
}

# getDistributions

`getDistributions` is a method on the `ARIO` class that returns the rewards distribution information for the specified epoch. If no epoch index is provided, the current epoch is used.

`getDistributions` does not require authentication.

## Parameters

<Table>
  <Table.Head>
    <Table.Row>
      <Table.Header>Parameter</Table.Header>
      <Table.Header>Type</Table.Header>
      <Table.Header>Description</Table.Header>
      <Table.Header>Required</Table.Header>
    </Table.Row>
  </Table.Head>
  <Table.Body>
    <Table.Row>
      <Table.Cell className="font-semibold">epochIndex</Table.Cell>
      <Table.Cell>number</Table.Cell>
      <Table.Cell>
        The epoch index for which to retrieve distribution data.
      </Table.Cell>
      <Table.Cell>No</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

## Examples

<CodeGroup title="getDistributions">

    ```typescript {{ title: 'NodeJS' }}
    const { ARIO } = require('@ar.io/sdk');

    async function main() {
      const ario = ARIO.init();
      const distributions = await ario.getDistributions({ epochIndex: 0 });

      console.log(distributions);
    }

    main();
    ```

    ```typescript {{ title: 'Web' }}
    import { ARIO } from '@ar.io/sdk/web';

    async function main() {
      const ario = ARIO.init();
      const distributions = await ario.getDistributions({ epochIndex: 0 });

      console.log(distributions);
    }

    main();
    ```

</CodeGroup>

## Output

```json
{
  "totalEligibleGateways": 1,
  "totalEligibleRewards": 100000000,
  "totalEligibleObserverReward": 100000000,
  "totalEligibleGatewayReward": 100000000,
  "totalDistributedRewards": 100000000,
  "distributedTimestamp": 1720720621424,
  "rewards": {
    "eligible": {
      "IPdwa3Mb_9pDD8c2IaJx6aad51Ss-_TfStVwBuhtXMs": {
        "operatorReward": 100000000,
        "delegateRewards": {}
      }
    },
    "distributed": {
      "IPdwa3Mb_9pDD8c2IaJx6aad51Ss-_TfStVwBuhtXMs": 100000000
    }
  }
}
```

## Try it out

<CodeExecutor
 title="getDistributions()"
 language="javascript"
 code={`// Example: Get distributions for epoch 140
 async function main() {
   try {
     // ARIO is injected by the runner in this environment
     const ario = ARIO.init();
     
     console.log('Fetching distributions for epoch 140...');
     
     // Make the actual API call
     const distributions = await ario.getDistributions({ epochIndex: 140 });
     
     console.log('Distributions:');
     console.log(JSON.stringify(distributions, null, 2));
     return distributions;
     
   } catch (error) {
     console.error('Error:', error.message);
     throw error;
   }
 }
 
 // Run the example
 main();`}
/>

## Try it out (Node.js Environment)

This example runs in a full Node.js environment using WebContainer, allowing you to use real npm packages and Node.js APIs.

> **Note:** WebContainer requires specific CORS headers that may not be available in all environments. If you encounter initialization errors, the browser-based CodeExecutor above will work in all environments.

<WebContainerExecutor
title="getDistributions() - Node.js"
language="javascript"
code={`// Example: Get distributions for epoch 140 using Node.js environment
// This runs in a full Node.js environment with real npm packages

async function main() {
try {
console.log('Node.js Environment Test');
console.log('Node version:', process.version);
console.log('Platform:', process.platform);
console.log('Architecture:', process.arch);

    console.log('\\nLoading ARIO SDK...');

    // Initialize the ARIO SDK
    const { ARIO } = await import('@ar.io/sdk');
    const ario = ARIO.init();

    console.log('✅ ARIO SDK loaded successfully');
    console.log('\\nFetching distributions for epoch 140...');

    // Make the actual API call
    const distributions = await ario.getDistributions({ epochIndex: 140 });

    console.log('✅ Distributions fetched successfully');
    console.log('\\nDistributions:');
    console.log(JSON.stringify(distributions, null, 2));

    // Additional Node.js specific features
    console.log('\\n--- Node.js Environment Info ---');
    console.log('Current working directory:', process.cwd());
    console.log('Environment variables available:', Object.keys(process.env).length);
    console.log('Memory usage:', process.memoryUsage());

    return distributions;

} catch (error) {
console.error('❌ Error:', error.message);
console.error('Stack trace:', error.stack);
throw error;
}
}

// Run the example
main().then(result => {
console.log('\\n✅ Execution completed successfully');
}).catch(error => {
console.error('\\n❌ Execution failed');
process.exit(1);
});`}
/>
